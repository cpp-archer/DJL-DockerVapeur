services : 
  vapeur:
    build: .
    container_name: Vapeur
    ports:
    - "3005:3005"
    volumes:
      - ./prisma/database.db:/app/prisma/database.db

    env_file:
      - .env
      
    command: sh -c "npx prisma migrate deploy && npm start"
